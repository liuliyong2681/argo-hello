# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: swr.cn-east-2.myhuaweicloud.com/zhenyun-open/nginx
  pullPolicy: IfNotPresent
  tag: "latest"

